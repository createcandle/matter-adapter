{
  "author": "CandleSmartHome.com",
  "description": "Adds support for Matter devices",
  "content_scripts": [
    {
      "css": [
        "css/extension.css"
      ],
      "js": [
        "js/extension.js"
      ]
    }
  ],  
  "gateway_specific_settings": {
    "webthings": {
      "exec": "python3 {path}/main.py",
      "primary_type": "adapter",
      "strict_max_version": "*",
      "strict_min_version": "1.1.0"
    }
  },
  "homepage_url": "https://github.com/createcandle/matter-adapter",
  "id": "matter-adapter",
  "license": "",
  "manifest_version": 1,
  "name": "Matter adapter",
  "options": {
    "default": {
      "Debugging": false,
	  "Use Hotspot addon as WiFi network for devices": true
    },
    "schema": {
      "properties": {
        "WiFi name": {
            "description": "What is the name of the WiFi network the Matter devices should connect to (if they use WiFi).",
            "type": "string"
        },
        "WiFi password": {
            "description": "What is the password of the WiFi network?",
            "type": "string"
        },
		"Use Hotspot addon as WiFi network for devices":{
            "type": "boolean",
            "description": "It is recommended to use the Hotspot addon to generate a separate guest WiFi network for smart devices that connect using Wi-Fi. Doing so will give you more control over the communication habits of your Matter devices."
		},
        "Debugging": {
          "type": "boolean",
          "description": "Advanced. Will generate details about the addon's processes in the internal log."
        }
      },
      "required": [],
      "type": "object"
    }
  },
  "short_name": "matter",
  "version": "0.0.11",
  "web_accessible_resources": [
    "css/*.css",
    "images/*.svg",
	"images/*.png",
	"images/*.gif",
	"images/*.jpg",
    "js/*.js",
    "views/*.html"
  ]
}
