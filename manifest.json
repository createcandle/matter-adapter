{
  "author": "CandleSmartHome.com",
  "description": "Adds support for Matter devices",
  "content_scripts": [
    {
      "css": [
        "css/extension.css",
		"css/matter-dataviz.css"
      ],
      "js": [
        "js/extension.js",
        "js/qrcode.js",
		"js/d3.min.js"
      ]
    }
  ],  
  "gateway_specific_settings": {
    "webthings": {
      "exec": "python3 {path}/main.py",
      "primary_type": "adapter",
      "strict_max_version": "*",
      "strict_min_version": "1.1.0"
    }
  },
  "homepage_url": "https://github.com/createcandle/matter-adapter",
  "id": "matter-adapter",
  "license": "",
  "manifest_version": 1,
  "name": "Matter adapter",
  "options": {
    "default": {
	  "Region":"EU",
	  "Help Thread devices to connect to the internet": false,
	  "Create extra properties": false,
      "Brightness transition duration":0,
	  "Days between updating certificates":14,
	  "Thread channel":26,
	  "Thread network name": "CandleThread",
	  "Default Matter fabric name": "Home",
	  "Matter server type": "NodeJS",
	  "Enable Matter server dashboard": false,
	  "Use Candle meshlocalprefix when creating a new Thread network": false,
      "Debugging": false
    },
    "schema": {
      "properties": {
        "Region":{
            "type": "string",
            "enum": [
                "EU",
                "US"
            ],
            "description": "How and how strongly your Thread radio is allowed to broadcast depends on which region you're in. The default setting is Europe (EU)."
        },
        "Help Thread devices to connect to the internet": {
            "type": "boolean",
            "description": "Enabling this is not recommended, unless you have a Thread device that absolutely needs it. Note that firmware updating does not require this to be enabled. The default is to not allow this."
        },
        "Create extra properties": {
            "type": "boolean",
            "description": "Enabling this will cause some things to gain additional properties which are not part of the Matter standard. This can be useful to create more advanced automations around Variables, for example. This feature is disabled by default."
        },
        "Brightness transition duration": {
            "description": "If you would like your lights brightness transitions to not be instantaneous you can set a specific speed in seconds here. The default is 0.",
            "type": "integer",
            "minimum": 0,
            "maximum": 10
        },
        "Days between updating certificates":{
            "description": "During pairing Matter depends on certicates that validate official Matter devices. Whenever you start a pairing process, and a few days have passed, then this addon will try to download the latest certificates from csa-iot.org. What's the minimum amount of days before an update should be attempted? The default is 14 days. Set it to zero to fully disable updating (which means you will unable to pair newly released Matter devices).",
            "type": "integer",
            "minimum": 0,
            "maximum": 28
        },
        "Thread channel":{
            "description": "Advanced. Which wireless channel the Thread network should use. Common recommendations are 11, 15, 20 and 25 or 26. Fun fact: Apple always uses 26. The default is 26.",
            "type": "integer",
            "minimum": 11,
            "maximum": 26
        },
        "Thread network name":{
            "description": "Advanced. Set a custom name for the Thread network. Leave empty to use the default, which is 'CandleThread'.",
            "type": "string"
        },
        "Matter server type":{
            "type": "string",
            "enum": [
  			  "Python",
              "NodeJS"
            ],
            "description": "Advanced. You can choose which Matter server to run, the old but stable Python version, or the new but barely tested Node JS version (which also uses more memory). The default is Python."
        },
        "Default Matter fabric name":{
            "type": "string",
            "description": "Advanced. Set a custom default Matter fabric name. If left empty the default name will be 'Home'."
        },
        "Enable Matter server dashboard": {
          "type": "boolean",
          "description": "Advanced. The Matter Server comes with it's own little webserver (on port 5580) that offers details about the Matter network. This feature is disabled by default, but you can enable it here."
        },
		"Use Candle meshlocalprefix when creating a new Thread network":{
            "type": "boolean",
            "description": "Advanced. Normally whenever a brand new Thread dataset is created it gets a random meshlocalprefix. If you enable this however, then the meshlocalprefix will always be set to fd00:ca4d:1e00:0::"
		},
        "Debugging": {
          "type": "boolean",
          "description": "Advanced. Will generate details about the addon's processes in the internal log."
        }
      },
      "required": [],
      "type": "object"
    }
  },
  "short_name": "matter",
  "version": "0.9.4",
  "web_accessible_resources": [
    "css/*.css",
    "images/*.svg",
    "images/*.png",
    "images/*.gif",
    "images/*.jpg",
    "js/*.js",
    "views/*.html",
	"other/*.json",
	"other/*.xml"
  ]
}
